public class BusinessAddress extends Address<BusinessAddress>{
    BusinessAddress(String street, String city, String state, String zip, String country) {
        super(street, city, state, zip, country);
    }
}
