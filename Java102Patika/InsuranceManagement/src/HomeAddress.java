public class HomeAddress extends Address<HomeAddress>{
    HomeAddress(String street, String city, String state, String zip, String country) {
        super(street, city, state, zip, country);
    }
}
